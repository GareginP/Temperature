


//function caltemp(Celsius){
   // var temperature= Number(prompt("Enter Celsius temperature to convert to Fahrenheit:"));
   // var temperature= Cel;
   // var result= Fahrenheit = (Celsius * 9/5) + 32;
   // console.log(result);
//}

function caltemp(valNum) {
    var temperature= Number(prompt("Enter Celsius temperature to convert to Fahrenheit:"));
    Celsius=temperature
    document.getElementById("outputFahrenheit").innerHTML = Fahrenheit = (Celsius * 9/5) + 32;
}